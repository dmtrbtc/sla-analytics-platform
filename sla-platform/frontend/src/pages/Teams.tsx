import { Typography } from "antd";

export default function Teams() {
  return (
    <div>
      <Typography.Title level={4}>Teams Configuration</Typography.Title>
      <Typography.Text type="secondary">Team management will be available here.</Typography.Text>
    </div>
  );
}
