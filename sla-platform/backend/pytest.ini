[pytest]
asyncio_mode = auto
testpaths = tests
addopts = -m "not integration" -ra
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: marks tests requiring external services (backend / PostgreSQL)
